name: Deploy Prebuilt React App

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Save and Restrict Permissions on Private Key
        run: |
          echo "${{ secrets.HOSTINGER_PRIVATE_KEY }}" > private_key.pem
          chmod 600 private_key.pem

      - name: Test SSH Connection
        run: ssh -i private_key.pem -p 65002 u878508099@46.202.183.38 echo "SSH connected successfully"

      - name: Debug Private Key
        run: |
          echo "${{ secrets.HOSTINGER_PRIVATE_KEY }}" > private_key.pem
          ssh-keygen -y -f private_key.pem

      - name: Deploy Prebuilt Static Files to Hostinger
        uses: appleboy/scp-action@v0.1.3
        with:
          host: 46.202.183.38
          port: 65002
          username: u878508099
          key: ${{ secrets.HOSTINGER_PRIVATE_KEY}}
          target: /public_html/react-one
          source: dist/*
          debug: true

      - name: Clean Up and Finalize on Hostinger
        uses: appleboy/ssh-action@v0.1.1
        with:
          host: 46.202.183.38
          port: 65002
          username: u878508099
          key: ${{ secrets.HOSTINGER_PRIVATE_KEY }}
          script: |
            cd /public_html/react-one
            echo "Deployment complete."
